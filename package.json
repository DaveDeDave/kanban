{
  "name": "kanban",
  "version": "1.0.0",
  "description": "Kanban task management web app challenge from frontend mentor",
  "repository": "https://github.com/DaveDeDave/kanban",
  "author": "Davide Ceci",
  "license": "MIT",
  "private": true,
  "packageManager": "yarn@3.3.0",
  "workspaces": {
    "packages": [
      "./packages/*"
    ]
  },
  "scripts": {
    "docker:start:dev": "docker-compose -f docker-compose.dev.yml up -d --build",
    "docker:stop:dev": "docker-compose -f docker-compose.dev.yml down"
  },
  "devDependencies": {
    "prettier": "^2.7.1"
  }
}
